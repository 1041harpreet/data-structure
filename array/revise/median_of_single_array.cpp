// int find_median(vector<int> v)
// 		{
// 		   sort(v.begin(), v.end());
    
//     if (v.size() % 2 == 0) {
//         int mid = v.size() / 2 - 1; // Adjust the index to get the correct mid element
//         return (v[mid] + v[mid + 1]) / 2;
//     } else {
//         return v[v.size() / 2];
//     }
// 		}